# k8s/kafka-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: kafka-headless
  namespace: default
spec:
  clusterIP: None # Headless Service для прямого доступа к каждому поду [citation:5]
  selector:
    app: kafka
  ports:
    - name: client
      port: 9092
      targetPort: 9092