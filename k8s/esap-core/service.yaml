# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: esap-service
  namespace: default
spec:
  type: ClusterIP  # Внутренний доступ
  selector:
    app: esap
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP